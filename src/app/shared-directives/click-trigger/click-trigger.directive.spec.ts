import { ClickTriggerDirective } from './click-trigger.directive';

describe('ClickTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
